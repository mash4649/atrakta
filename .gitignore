# Build / cache (Go, local fixture runs)
.tmp/
tmp/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/

# Go workspace (optional)
go.work
go.work.sum

# OS
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
.cursor/

# Local bundles / large workspace artifacts
*.bundle

# Workspace-only trees (not published with this repo)
version-snapshots-local/

# Atrakta managed (parent workspace tooling; keep out of this repo)
/.atrakta/git/checkpoint-latest.json
/.atrakta/git/bootstrap.md

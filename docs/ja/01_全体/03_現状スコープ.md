# 現状スコープ

[English](../../en/01_全体/03_現状スコープ.md) | [日本語](./03_現状スコープ.md)


## 実装済み（Phase1/Phase2）

- `AGENTS.md` 自動生成（未作成時）
- sync level 0/1/2 と proposal-only 運用
- optional projection templates
  - `contract-json`
  - `atrakta-link`
- gate quick/heavy checks 拡張
- migrate replay determinism 検証
- Linux/Windows 向けクロスビルド検証
- Linux バイナリ実行検証（Docker経由）
- サブワーカー自律判定（Single Writer, Multi Worker）
- `single_writer_queue` による決定論統合（提案並列 -> 単一適用）
- DAG task graph 実行（`task_id` / `task_blocked_by` / `.atrakta/task-graph.json`）
- `state/events` の lock + atomic 更新
- 非破壊 Git 自動化（auto初期化、snapshot、checkpoint）
- events schema v2 固定（旧schema fail-closed）
- edit safety（Go AST / JSON parse）による write/copy 事前検証
- run checkpoint 保存と `resume` による再開導線
- `gc` コマンドによる `.tmp` 運用GC（閾値型）と `events` proposal-only 提案
- `context.conventions` の常時読込と read-only 保護
- Conventions 段階読込（index -> 優先節）によるトークン効率化
- `repo_map` 予算/更新間隔制御（runtime cache 付き）
- Start Snapshot Fast Gate（`start_fast_v2`）で strict path を条件付きスキップ
- `start` 高速化（events 検証キャッシュ + サブワーカーイベントの batch/group append）
- wrapper ハイブリッド stamp（軽量判定 + deep fallback + 定期深検査）
- `start` / `doctor` 実行メトリクスの runtime 記録（`runtime.json`）
- release 自動化（CalVer tag + artifact + auto release notes）

## 未対応・将来拡張

- Windows ネイティブ実行テスト（現在環境では未実施）
- branch lane の「計画」から先の運用自動化（導入する場合も Single Writer 原則は維持）
- 品質ゲートの言語/プロジェクト種別別最適化
